import styled from 'styled-components';

export const LoaderWrapper = styled.div`
	width: 100%;
  position: absolute;
	top: 0;
	left: 0;
	right: 0;
	bottom: 0;
  display: flex;
  justify-content: center;
	align-items: center;
	user-select: none;
`;
